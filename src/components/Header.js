import React, {Component} from "react";

class Header extends Component {
    render() {
        return (
            <header id="header">CV Builder App</header>
        );
    }
}

export default Header;